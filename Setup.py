from setuptools import setup

setup(
    name='your-package-name',
    version='0.1',
    packages=['your_package'],
    install_requires=[
        'elevenlabs',
        'openai',
        'numpy',
        'sounddevice',
        #For now..
    ],
)
